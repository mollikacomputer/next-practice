import React from 'react';
import useSomething from '@/hooks/useSomething';

const HeroSection = () => {
    return (
        <div>
            <h2> Hero Section </h2>
        </div>
    );
};

export default HeroSection;